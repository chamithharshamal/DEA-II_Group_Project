spring.application.name=staff-service
server.port=8089

# --- EUREKA ---
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# --- DATABASE CONFIG (FIXED) ---

# 1. URL: Only the address + SSL mode (No user/pass here!)
spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require

# 2. Username: Taken from your link
spring.datasource.username=postgres.hmkftierrycxscktqdwi

# 3. Password: Put your password here
spring.datasource.password=[Silu@#098SD]

# --- JPA SETTINGS ---
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect